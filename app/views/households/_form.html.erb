<div class="form">
	<%= form_for(@household) do |f| %>

		<label>Description</label>
		<%= f.text_field :name %><br>
		<label>Address</label>
		<%= f.text_field :address %><br>

		<br>
		<%= f.submit "Submit Household" %>
	<% end %>
</div>